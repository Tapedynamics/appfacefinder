<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Volti Riconosciuti</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            max-width: 900px;
            margin-top: 50px;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
        }
        .face-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }
        .face-item:last-child {
            border-bottom: none;
        }
        .face-id {
            font-weight: bold;
            color: #007bff;
            word-break: break-all; /* Per ID lunghi */
        }
        .photo-count {
            color: #6c757d;
            white-space: nowrap; /* Evita che il conteggio vada a capo */
        }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="text-center mb-4">Admin Panel - Volti Riconosciuti</h2>

        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}

        <div class="mb-3">
            <a href="{{ url_for('admin') }}" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Torna al Pannello Admin</a>
        </div>

        {% if faces %}
            <ul class="list-group">
                {% for face_id, count in faces %}
                    <li class="list-group-item face-item">
                        <div>
                            <span class="face-id">ID Volto: {{ face_id }}</span>
                            <span class="photo-count">({{ count }} foto)</span>
                        </div>
                        <a href="{{ url_for('admin_face_photos', face_id=face_id) }}" class="btn btn-sm btn-primary">
                            <i class="bi bi-eye"></i> Visualizza Foto
                        </a>
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p class="text-center">Nessun volto riconosciuto nel database.</p>
        {% endif %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
